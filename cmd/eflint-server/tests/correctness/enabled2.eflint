  Fact pair Identified by string * int Conditioned by string != Olaf.
  +pair(Olaf, 21).
  +pair(Bob, 22).
  ?!Enabled(pair(Olaf, 21)).
  ?Enabled(pair(Bob, 22)).
  Fact test Identified by String Conditioned by test != Olaf.
  +test(Olaf).
  +test(Bob).
  ?!Enabled(test(Olaf)).
  ?Enabled(test(Bob)).